{
  "name": "parivahan-rto-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx tsx backend/server.ts",
    "dev:separated": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,green\" \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && next build",
    "start": "NODE_ENV=production npx tsx backend/server.ts",
    "seed": "cd backend && npm run seed",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "unrs-resolver@1.12.2": true
  }
}
